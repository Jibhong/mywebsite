steps:
  - name: 'ubuntu'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        echo "=== cloudbuild.yaml inside repo is being used ==="
        echo "PROJECT_ID: $PROJECT_ID"
        echo "BUILD_SECRET length: $${#BUILD_SECRET}"
    secretEnv: ['BUILD_SECRET']
